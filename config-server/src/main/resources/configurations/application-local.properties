# Shared local profile settings (applied to ALL services when profile=local)
# Register with Eureka using host.docker.internal so the Docker-based API Gateway
# can route requests back to the service running on the host machine (IDE).
eureka.instance.hostname=host.docker.internal
eureka.instance.prefer-ip-address=false
